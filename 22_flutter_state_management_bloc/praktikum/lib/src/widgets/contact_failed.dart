import 'package:flutter/material.dart';

class ContactFailed extends StatelessWidget {
  const ContactFailed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Failed'),
    );
  }
}
