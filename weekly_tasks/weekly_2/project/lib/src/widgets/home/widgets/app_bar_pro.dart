import 'package:flutter/material.dart';

class AppBarPro extends StatelessWidget {
  const AppBarPro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [
          Text('Hello World'),
        ],
      ),
    );
  }
}
